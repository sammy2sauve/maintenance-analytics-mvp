name: Keep Render alive

on:
  schedule:
    - cron: '*/14 * * * *'   # every 14 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -sf https://maintenance-analytics-mvp.onrender.com/health || true
